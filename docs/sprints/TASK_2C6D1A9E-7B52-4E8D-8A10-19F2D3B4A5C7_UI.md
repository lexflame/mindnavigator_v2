# UI Notes: TASK_2C6D1A9E-7B52-4E8D-8A10-19F2D3B4A5C7

## Title
ProjectsNav UI tree rendering.

## Implemented
1. Hierarchical rendering in `ProjectsNav`:
- Replaced flat project listing logic with tree flattening by `parent_project_id`.
- Root projects are sorted by `(area, priority, title)`; children by `(sort_order, priority, title)`.
- Added indentation and marker icons:
  - `▾` expanded parent,
  - `▸` collapsed parent,
  - plain entry for leaf.

2. Expand/collapse state handling:
- Added in-memory state store: `_collapsed_project_ids`.
- Added double-click toggle for parent project nodes.
- State persists across list repopulation and mode changes within session.

3. Backward-compatible filter behavior:
- Existing `project_filter_changed` and `filter_changed` signals are preserved.
- Selection/fallback logic remains compatible with existing workspaces.

## Validation
1. `python -m compileall mindnavigator/ui/projects_nav.py` passed.
