# TASK_7A4D29C6-8B51-4F0A-90D1-2C5E7B8F1D54 Plan

## Scope
- Add attach and detach flows between dossier items and supported entity kinds: `task`, `map`, `marker`, `note`, `idea`, `object`, `character`.
- Keep the implementation symmetrical with the existing character attachment pattern instead of introducing a second relationship workflow.
- Limit UI work to Dossier workspace entry points and preview links area; keep details dialog read-only for this wave.

## Dependencies
- Existing dossier storage schema and CRUD from Waves 1-3.
- Existing entity fetch APIs used by character links to provide picker options and human-readable labels.
- Existing Dossier workspace preview pane and selection lifecycle.

## Implementation Notes
- Add a storage helper that returns linkable target options per entity kind, with search filtering and stable sorting.
- Extend the Dossier workspace preview links card with add/remove controls and a small modal picker dialog.
- Store link ids and target metadata on link list items so detach and info behaviors stay cheap and deterministic.
- Reuse current confirmation and status patterns; avoid broad workspace restructuring.

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_dossier_workspace.py tests/test_dossier_storage.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_dossier_p4`

## Rollback
- Revert Dossier link option query and workspace attach/detach UI hooks.
- Leave existing dossier records and schema intact if Wave 4 changes need to be backed out.
