# AGENTS.md

Main project instructions are stored in `.codex/AGENTS.md`.
Before making any changes, read `.codex/AGENTS.md` and follow it as the primary project instruction file.
If this file conflicts with `.codex/AGENTS.md`, `.codex/AGENTS.md` takes precedence.

## Purpose
Compatibility bridge for tools that auto-discover only the repository-root `AGENTS.md`.
The consolidated repository policy now lives in `.codex/AGENTS.md` and should be treated
as the single authoritative instruction file for this project.

## Rule Priority
1. Direct task request.
2. `.codex/AGENTS.md`.
3. `.codex/SKILL.md` and active skill files.
4. Other supporting `.codex/*` reference documents.

## Required Action
Before acting on repository work, read `.codex/AGENTS.md` and follow it as the authoritative policy.
Keep this bridge aligned with `.codex/AGENTS.md` if the instruction structure changes.

