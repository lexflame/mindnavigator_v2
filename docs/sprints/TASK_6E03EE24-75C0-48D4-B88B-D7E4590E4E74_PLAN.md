# Task Plan: TASK_6E03EE24-75C0-48D4-B88B-D7E4590E4E74

## Title
Split `mindnavigator/workspaces/tasks/module_impl.py` into real class files and include the pending tasks quick-action text fix.

## Scope
1. Move task workspace class implementations from `module_impl.py` into the existing per-class files under `mindnavigator/workspaces/tasks/`.
2. Keep legacy imports working through a compatibility `module_impl.py` aggregator.
3. Preserve current runtime behavior and include the pending delegate text rendering change already present in the working tree.

## Dependencies
1. Shared task dataclasses, item roles, constants, and helper functions currently live beside the classes and must remain importable without introducing cycles.
2. Existing package exports and legacy alias modules must continue to resolve `TasksWorkspace` and related classes.
3. Focused task workspace tests must continue to pass against the new layout.

## Validation
1. `python -m compileall mindnavigator main.py`
2. Focused `pytest` for task workspace module split and delegate/workspace behavior.

## Rollback Notes
1. Revert the task package split files and restore the previous monolithic `module_impl.py`.
2. Keep the quick-action text fix attached to the same task so rollback is one change set, not two unrelated edits.
