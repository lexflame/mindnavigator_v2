# TASK_2F0C63E8-21E8-4E43-8E62-7B7D12E7B632 Plan

## Scope
- Add a dedicated `mindnavigator/workspaces/dossier/` workspace package with a tasks-like shell and list UX.
- Register Dossier mode in the main window, left rail, i18n mode labels, and workspace visibility settings.
- Keep the first pass focused on shell, list rendering, and mode switching without pulling in Wave 3 editor/detail surfaces.

## Dependencies
- `docs/sprints/10_SPRINT.md`
- Wave 1 storage API in `mindnavigator/storage/`
- Existing workspace shell patterns in `mindnavigator/ui/workspaces/base_workspace.py` and list/delegate patterns in current workspaces

## Implementation Notes
- Build the shell on top of `BaseWorkspace` and keep project navigation side effects out of the Dossier workspace instead of refactoring global nav in this wave.
- Use one list model and one delegate for all dossier kinds, with kind/status/rating filters plus search text.
- Limit integration to additive mode registration and visibility wiring so existing modes remain stable.

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_workspace_visibility_settings.py tests/test_dossier_workspace.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_dossier_p2`

## Rollback
- Revert the new Dossier workspace package, main-window mode registration, and focused tests.
- Leave storage schema and unrelated workspaces untouched.
