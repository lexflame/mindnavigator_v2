# Hotfix Notes: TASK_0DE2B002-565F-4A74-9AD9-00668A7AA79A

## Title
Enable project drag&drop reorder in ProjectsWorkspace list.

## Root Cause
Drag&drop logic was implemented in `ProjectsNav`, but main project operations happen in `ProjectsWorkspace` (`QListView`), where DnD was not implemented.

## Fix
1. Added `_ProjectsListView` with internal drag/drop handling in `projects_workspace.py`.
2. Enabled drag/drop mode and move action on workspace list.
3. Extended `ProjectsModel.flags()` with drag/drop flags for project rows.
4. Added `ProjectsModel.move_project_by_drop(...)` using storage `move_project(...)`.
5. Added workspace handler `_handle_project_drop(...)` to apply move and restore selection.
6. Added `as_child` drop mode:
- middle-row drop with right horizontal offset reparents source under target;
- top/bottom drop zones reorder among siblings.

## Validation
1. `python -m compileall mindnavigator/workspaces/projects_workspace.py` passed.
