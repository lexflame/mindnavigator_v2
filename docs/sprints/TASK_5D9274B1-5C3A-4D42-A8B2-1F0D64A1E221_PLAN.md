# TASK_5D9274B1-5C3A-4D42-A8B2-1F0D64A1E221 Plan

## Scope
- Add additive SQLite schema for Dossier records, typed metadata, and generic dossier links.
- Add storage dataclasses and `Database` CRUD/fetch helpers for Dossier entities.
- Keep the implementation isolated to `mindnavigator/storage/` plus focused tests.

## Dependencies
- `docs/sprints/10_SPRINT.md`
- Existing storage mixin architecture in `mindnavigator/storage/`
- Existing migration helpers in `mindnavigator/spaceenity/db_migrations.py`

## Implementation Notes
- Use a dedicated storage mixin instead of expanding unrelated mixins.
- Store typed metadata as JSON with kind-aware normalization so Wave 2 and Wave 3 can reuse one persistence shape.
- Add validation for supported dossier kinds and supported link targets: `task`, `map`, `marker`, `note`, `idea`, `object`, `character`.
- Keep timestamps in UTC ISO format via `datetime.now(timezone.utc)`.

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_db_migrations.py tests/test_dossier_storage.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_dossier_p1`

## Rollback
- Revert dossier-specific storage files, schema migration step, and tests.
- Leave existing storage tables and unrelated workspace code untouched.
