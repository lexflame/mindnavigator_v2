# TASK_9C1E6A42-3D84-46FE-9A25-6E3B7C2D5F65 Plan

## Scope
- Add missing discovery controls for Dossier mode: tag filter, optional grouping, and aggregate summary output.
- Keep the implementation inside the existing Dossier workspace and list model instead of introducing a second navigation surface.
- Preserve Wave 4 attach/detach behavior and existing create/edit/details flows.

## Dependencies
- Existing Dossier CRUD, metadata preview helpers, and attachment preview card from Waves 1-4.
- Current `BaseWorkspace` search/filter persistence behavior.
- Existing grouped-list patterns from notes and ideas workspaces for category headers.

## Implementation Notes
- Extend the Dossier list model with optional group headers for `kind`, `status`, or `rating`.
- Add a tag text filter and a grouping combo to the workspace filter row, persisted through workspace state.
- Add a compact summary strip that reports filtered totals and aggregate counts by kind, status, rating, and top tags.
- Keep grouping derived from already fetched dossier rows so no schema or storage contract changes are needed.

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_dossier_workspace.py tests/test_dossier_storage.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_dossier_p5`

## Rollback
- Revert Dossier grouping helpers, filter controls, and summary rendering without touching storage or Wave 4 links.
- Leave the existing unified Dossier CRUD and attachment layer intact if Wave 5 UX needs to be backed out.
