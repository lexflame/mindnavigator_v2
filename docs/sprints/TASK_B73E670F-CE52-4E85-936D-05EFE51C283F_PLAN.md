# TASK_B73E670F-CE52-4E85-936D-05EFE51C283F

## Title
Refactor BOARD mode so task day filtering is optional and exposed as a separate control.

## Scope
- Audit the current BOARD data path that always limits cards to the focused day.
- Add a dedicated `Фильтрация по дню` control for BOARD mode instead of coupling the board to the day focus unconditionally.
- Keep GANTT and DASH tied to `focus_day` while allowing BOARD to show all active tasks when day filtering is disabled.
- Update BOARD labels and tests so multi-day cards remain readable and the new toggle is covered.

## Dependencies
- `mindnavigator/workspaces/tasks/tasks_workspace.py`
- `tests/test_tasks_workspace_mn202.py`
- `.codex/HISTORY_TASK.md`
- `.codex/HISTORY_ACTION.md`

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_tasks_workspace_mn202.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_tasks_board_day_filter`

## Rollback
- Revert the BOARD-specific checkbox and fetch-path changes if multi-day cards or secondary mode switching regress.
- Keep the existing `focus_day` logic intact for GANTT and DASH if only BOARD behavior needs rollback.

## Notes
- BOARD currently reuses a helper that always filters active tasks by `focus_day`; this is the main coupling to remove.
- The separate day-filter option should remain explicit in the UI and should not change task status semantics or drag-drop behavior.
