# TASK_2887FA24-5C8B-468B-9C68-60A417F7AF11

## Scope
- Extract Tasks secondary modes `BOARD`, `GANTT`, and `DASH` from `tasks_workspace.py` into dedicated helper classes.
- Move Tasks workspace style and custom paint logic into a dedicated style helper.
- Keep current Tasks behavior stable, including the recent BOARD day-filter option and runtime theme switching.

## Decomposition
1. Create helper packages:
   - `mindnavigator/workspaces/tasks/cast_board`
   - `mindnavigator/workspaces/tasks/cast_gantt`
   - `mindnavigator/workspaces/tasks/cast_dash`
   - `mindnavigator/workspaces/tasks/style`
2. Extract mode-specific widget building and refresh logic into helper classes.
3. Extract stylesheet generation, gantt palette application, and custom chart/bar paint code into `TasksWorkspaceStyle`.
4. Rewire `TasksWorkspace` to delegate mode work and theme fan-out through the helpers.
5. Add regression coverage for the new helper wiring and rerun focused Tasks/theme validation.

## Dependencies
- `mindnavigator/workspaces/tasks/tasks_workspace.py`
- `mindnavigator/workspaces/tasks/tasks_item_delegate.py`
- `tests/test_tasks_workspace_mn202.py`
- `tests/test_theme_switch_runtime.py`

## Validation
- `python -m compileall mindnavigator main.py`
- `PYTHONPATH=. pytest tests/test_tasks_workspace_mn202.py tests/test_theme_switch_runtime.py -p no:cacheprovider --basetemp .pytest_dir/run_tmp_tasks_casts_refactor`

## Rollback
- Revert the new helper packages and restore direct mode/style logic in `tasks_workspace.py`.
- Re-run the focused Tasks/theme validation suite to confirm parity after rollback.
